import { IShape, IText, IImage, IGroup } from '@/interfaces/layer'

export interface ICalculatedGroupStyle {
  x: number,
  y: number,
  width: number,
  height: number,
  initWidth: number,
  initHeight: number
}
